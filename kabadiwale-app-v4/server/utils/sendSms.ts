export async function sendSmsOtp(mobile: string, otp: string) {
    console.log(`Sending OTP "${otp}" to mobile number ${mobile}`);
    // Integrate with SMS gateway like Twilio, Fast2SMS, etc.
  }
  